[project]
name = "writings_with_code"
version = "0.1.0"
description = "Technical publications with code examples"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.0",
]
